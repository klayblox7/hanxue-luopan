# Tencent Cloud 테스트 배포 메모

작성일: 2026-06-15

## 목표

현재 작업 중인 홈페이지를 중국 내에서 VPN 없이 확인할 수 있도록 Tencent Cloud에 테스트 배포하고, 공유 가능한 주소를 사용자에게 전달한다.

## 현재 상황

- 사용자는 중국에서 생활 중이며, 중국 내 접속이 가능한 테스트 링크가 필요하다.
- GitHub Pages는 중국에서 VPN이 필요할 수 있어 이번 공유용 배포에는 사용하지 않는다.
- 사용자는 `https://cloud.tencent.com/`에 이미 로그인한 상태이며, Chrome에 해당 화면을 열어둔 상태라고 했다.
- 빨간색 박스로 표시한 다른 Codex 작업인 `오버레이 제거 및 PNG 축소`가 아직 진행 중이다.
- 이 작업이 끝난 뒤 최신 결과물로 배포해야 한다.

## 사용자가 명시적으로 요청한 일

1. `오버레이 제거 및 PNG 축소` 작업이 끝날 때까지 기다린다.
2. Chrome 자동화를 사용해 이미 로그인된 Tencent Cloud 화면으로 들어간다.
3. 현재 홈페이지 결과물을 Tencent Cloud에 업로드한다.
4. VPN 없이 공유 가능한 테스트 주소를 찾아 사용자에게 알려준다.

## 권장 배포 방식

우선순위:

1. Tencent EdgeOne Pages
   - GitHub Pages/Vercel과 가장 비슷한 정적 사이트 배포 방식.
   - 도메인 없이도 기본 제공되는 배포 주소를 공유할 수 있다.
   - `Preview` 임시 링크가 아니라 `Production`, `Access domain`, `.edgeone.app` 계열의 유지되는 링크를 공유해야 한다.

2. Tencent CloudBase Static Hosting
   - EdgeOne Pages 사용이 어렵거나 메뉴 접근이 막히면 대안으로 사용한다.
   - 기본 제공 도메인으로 테스트 공유가 가능하다.

## 업로드 전 체크

- 먼저 진행 중이던 `오버레이 제거 및 PNG 축소` 작업이 완료되었는지 확인한다.
- 최신 결과물이 반영되도록 필요한 경우 빌드한다.
- 정적 업로드 대상은 보통 `out` 폴더 또는 최종 정적 산출물 ZIP이다.
- 업로드하면 안 되는 항목:
  - `.git`
  - `node_modules`
  - `.next`
  - 임시 파일
  - 로그 파일
  - 개인 정보가 들어간 파일

## Chrome 작업 지침

- 사용자가 Chrome에 Tencent Cloud 로그인 상태를 준비해두었다고 했으므로 Chrome을 사용한다.
- 이미 열린 `https://cloud.tencent.com/` 탭이 있으면 그 탭을 이어받아 사용한다.
- 로그인, CAPTCHA, 휴대폰 인증, 결제, 유료 리소스 생성, 실명 인증, 권한 변경이 필요하면 사용자에게 먼저 확인한다.
- 홈페이지 정적 산출물을 Tencent Cloud에 업로드하는 것은 사용자가 요청한 범위 안에 있다.

## 완료 기준

- Tencent Cloud에 최신 홈페이지가 배포되어 있다.
- 중국 내에서 VPN 없이 접속 가능한 공유 주소를 확보했다.
- 사용자에게 아래 형식으로 알려준다.

```text
배포 완료했습니다.
공유 주소: https://...
업로드한 대상: ...
주의할 점: ...
```

## 참고

- 도메인과 ICP备案은 정식 운영 단계에서 고민하면 된다.
- 지금 목적은 테스트 공유이므로, Tencent Cloud에서 제공하는 기본 배포 주소를 우선 사용한다.
